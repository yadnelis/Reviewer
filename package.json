{
  "name": "reviewer",
  "version": "1.0.0",
  "description": "Easy Json based test review builder",
  "main": "Main.js",
  "scripts": {
    "dev-api": "node --env-file=.env.dev --watch main.js",
    "dev-app": "npm run dev --prefix ./reviewer.app",
    "dev": "run-p dev-api dev-app",
    "build": "npm run build --prefix ./reviewer.app --NODE_ENV=prod",
    "start": "node --env-file=.env.prod main.js"
  },
  "author": "Yadnelis",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "moment": "^2.30.1",
    "mongodb": "^6.16.0",
    "npm-run-all": "^4.1.5"
  }
}
